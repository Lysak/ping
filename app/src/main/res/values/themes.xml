<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Ping" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.Ping.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_animated</item>
        <item name="windowSplashScreenAnimationDuration">840</item>
        <item name="postSplashScreenTheme">@style/Theme.Ping</item>
    </style>
</resources>
