# Fail a hung test instead of blocking the run. Gradle's Test.timeout (2 min,
# in app/build.gradle.kts) is the hard backstop that kills a CPU-spinning worker.
junit.jupiter.execution.timeout.default = 60 s
junit.jupiter.execution.timeout.mode = disabled_on_debug
